# URL de base de l'API back-end (sans /api final, il est ajoute par le code).
# En local, ce fallback est deja code en dur dans src/api/client.js, cette
# variable n'est donc necessaire qu'en production (Vercel).
VITE_API_URL=http://localhost:4000/api
